# Single source of truth for "never let a model see this, never let git take it".
# Consumed by BOTH:
#   1. the server's layer-3 path denylist (paths.py), and
#   2. the docs/secrets gate (scripts/docs_gate.py).
# One list, two enforcers. Keep it that way -- a second copy will drift.
#
# Format: one fnmatch-style glob per line. Blank lines and # comments ignored.

.env
.env.*
# The registry names a host, so both it and the backup --init moves aside are denied.
# `.env.*` above already covers a .env backup; these are the other two.
#
# models.toml was deliberately left readable when the backup glob landed on 2026-09-08,
# on the grounds that .gitignore and NEVER_TRACK already stop it being committed. That
# reasoning covered the wrong surface: a delegated model's answer lands in a transcript
# and in the calling conversation, which is where pull request text gets drafted from, so
# reading the file puts the head node's name on a surface no hook gates. Nothing needs it
# -- models.toml.example is tracked and carries the same shape, --doctor and
# backend_status are operator-side, and a sandboxed command has no network to use an
# endpoint with. The example file stays readable, which is the control on this pair.
models.toml
models.toml.bak-*
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ecdsa*
id_ed25519*
*_rsa
*credential*
*secret*
*.kdbx
.netrc
.npmrc
.pypirc
.git/**
.ssh/**
.aws/**
.kube/**
.docker/config.json

# Endpoint captures (ADR-0052). Both enforcers are wanted here: the gate refuses to let
# one be tracked, and paths.py's layer 3 stops the local model reading one. The second is
# a deliberate cost, not an oversight -- it is why diff_endpoint_captures.py is a script
# and not a delegation.
local/endpoint-captures/*
local/endpoint-captures/**
