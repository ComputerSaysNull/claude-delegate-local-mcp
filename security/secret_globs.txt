# Single source of truth for "never let a model see this, never let git take it".
# Consumed by BOTH:
#   1. the server's layer-3 path denylist (paths.py), and
#   2. the docs/secrets gate (scripts/docs_gate.py).
# One list, two enforcers. Keep it that way -- a second copy will drift.
#
# Format: one fnmatch-style glob per line. Blank lines and # comments ignored.

.env
.env.*
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ecdsa*
id_ed25519*
*_rsa
*credential*
*secret*
*.kdbx
.netrc
.npmrc
.pypirc
.git/**
.ssh/**
.aws/**
.kube/**
.docker/config.json

# Endpoint captures (ADR-0052). Both enforcers are wanted here: the gate refuses to let
# one be tracked, and paths.py's layer 3 stops the local model reading one. The second is
# a deliberate cost, not an oversight -- it is why diff_endpoint_captures.py is a script
# and not a delegation.
local/endpoint-captures/*
local/endpoint-captures/**
