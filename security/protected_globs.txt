# Paths a host program acts on as configuration or instructions, without a person running
# anything and before a diff is read. Consumed by paths.py (the write tools refuse them)
# and sandbox.py (run_bash sees them read-only), both through paths.load_protected_globs.
# One list, two enforcers -- a second copy would drift, as secret_globs.txt says of itself.
#
# Format: secret_globs.txt's. One fnmatch-style glob per line, matched against every
# trailing run of path components, so `CLAUDE.md` matches one at any depth. A leading `!`
# exempts. Reasons for what is and is not listed: docs/specs/2026-09-23-host-acted-paths.md.

# Claude Code: settings and hooks, agents, skills, commands, and project MCP servers.
.claude/**
.mcp.json
CLAUDE.md
CLAUDE.local.md

# Editors: tasks, launch configurations and settings that name a program to run.
.vscode/**
.idea/**
