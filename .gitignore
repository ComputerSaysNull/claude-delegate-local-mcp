# --- secrets and machine-specific config (see security/secret_globs.txt) ---
.env
.env.*
!.env.example
models.toml
!models.toml.example
*.pem
*.key
id_*
security/forbidden_strings.txt

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# --- local scratch ---
# Benchmark probes: a WSL-created venv under /mnt/c contains symlinks that
# Git Bash cannot even index, so an accidental `git add -A` fails outright.
.bench/
.bench-venv/
.spike/
*.local.md

# Chunked scratch from ad hoc investigations. Twelve such files, ~1.35 MB of
# fragments of a proprietary compiled binary, were found untracked in the repo
# root with nothing in the repository referencing them. Root-anchored, so a
# legitimately tracked tmp_* elsewhere is unaffected.
/tmp_*

# Dated endpoint captures. Untracked by ADR-0052, because publishing what an endpoint
# returns is unrecoverable while CI-verifying a reference document is a small benefit.
# This is the convenience half; security/secret_globs.txt is the half that enforces.
/local/endpoint-captures/
